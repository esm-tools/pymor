# Rule-level parallelism diagnostic: same tossq_day rule duplicated as
# tossq_day_a / tossq_day_b, processing the SAME 3 GB SST input twice into
# two distinct output files. With parallel=True + pipeline_orchestrator=dask
# and N_WORKERS>=2, the two rules should land on separate workers and the
# sweep wall time should approach the single-rule time, not 2x.
#
# This isolates "does pycmor actually use the cluster for rule-level
# parallelism" from any compute/IO scaling questions inside one rule.

general:
  name: "hr-tossq-2x-parallel-bench"
  cmor_version: "CMIP7"
  mip: "CMIP"
  CMIP7_DReq_metadata: "/home/a/a270092/.cache/pycmor/cmip7_metadata/v1.2.2.4/metadata.json"

pycmor:
  enable_output_subdirs: true
  warn_on_no_rule: False
  # The two knobs the existing bench yamls have wrong:
  parallel: True
  pipeline_orchestrator: dask
  dask_cluster: "local"
  dask_n_workers: 2
  dask_threads_per_worker: 4
  dask_memory_limit: 96GB

inherit:
  data_path: &dp /work/bb1469/a270092/runtime/awiesm3-develop/Test_16n/outdata/fesom
  source_id: AWI-ESM3-4-2-veg-HR
  institution_id: AWI
  experiment_id: piControl
  parent_experiment_id: no parent
  variant_label: r1i1p1f1
  grid_label: g130
  mesh_path: &mp /work/ab0246/a270092/input/fesom2/dars2
  grid_file: /work/ab0246/a270092/input/fesom2/dars2/mesh.nc
  grid: "FESOM 2.6 unstructured grid DARS (3146761 surface nodes)"
  nominal_resolution: "10 km"
  institution: "Alfred Wegener Institute for Polar and Marine Research"
  output_directory: ./cmorized_output/bench_hr_tossq_2x

pipelines:
  - name: bench_square_pipeline
    steps:
      - pycmor.core.gather_inputs.load_mfdataset
      - pycmor.std_lib.generic.get_variable
      - script://$PYCMOR_HOME/examples/custom_steps.py:compute_square
      - pycmor.std_lib.timeaverage.timeavg
      - pycmor.std_lib.attributes.set_global
      - pycmor.std_lib.attributes.set_variable
      - pycmor.std_lib.attributes.set_coordinates
      - pycmor.std_lib.dimensions.map_dimensions
      - pycmor.core.caching.manual_checkpoint
      - pycmor.std_lib.generic.trigger_compute
      - pycmor.std_lib.generic.show_data
      - pycmor.std_lib.files.save_dataset

      - pycmor.std_lib.qc.run_compliance_checker
rules:
  - name: tossq_day_a
    inputs:
      - path: *dp
        pattern: sst\.fesom\.1587\.nc
    compound_name: ocean.tossq.tavg-u-hxy-sea.day.glb
    model_variable: sst
    squared_units: "degC2"
    lazy_write: true
    netcdf_compression_codec: blosc_zstd
    netcdf_compression_level: 3
    netcdf_write_scheduler: threads
    pipelines:
      - bench_square_pipeline

  - name: tossq_day_b
    inputs:
      - path: *dp
        pattern: sst\.fesom\.1587\.nc
    compound_name: ocean.tossq.tavg-u-hxy-sea.day.glb
    model_variable: sst
    squared_units: "degC2"
    lazy_write: true
    netcdf_compression_codec: blosc_zstd
    netcdf_compression_level: 3
    netcdf_write_scheduler: threads
    output_directory: ./cmorized_output/bench_hr_tossq_2x_b
    pipelines:
      - bench_square_pipeline
